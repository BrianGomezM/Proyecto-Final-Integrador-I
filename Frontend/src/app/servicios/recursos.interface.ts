/**
 * Interfaz que representa un recurso que contiene una lista de imágenes.
 */
export interface Recurso{
      /**
   * Lista de rutas de las imágenes asociadas al recurso.
   */
    imagenes:string[];
}